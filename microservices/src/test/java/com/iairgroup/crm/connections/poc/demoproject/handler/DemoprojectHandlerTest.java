package com.iairgroup.crm-connections.poc.demoproject.handler;

import org.junit.Before;
import org.junit.Test;
import com.iairgroup.crm-connections.poc.demoproject.handler.DemoprojectHandler;

import static org.junit.Assert.assertEquals;

/**
 * Unit tests for the DemoprojectHandler class.
 *
 * @author Your_Full_Name (Your_Bsafe)
 */
@RunWith(PowerMockRunner.class)
public class DemoprojectHandlerTest {

    /**
     *  Instance of class being tested
     */
    private DemoprojectHandler handler;

    /**
     * Set up needed for all tests
     */
    @Before
    public void setUp() {
        // Create an instance of the class being tested
        handler = new DemoprojectHandler();

        // Create the mocks
        // ADVICE: create any mocked objects here
    }


}
